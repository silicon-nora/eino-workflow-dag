name: Published package smoke

on:
  workflow_call:
    inputs:
      version:
        description: Exact npm package version to verify
        required: true
        type: string
  workflow_dispatch:
    inputs:
      version:
        description: Exact npm package version to verify
        required: true
        type: string

permissions:
  contents: read

jobs:
  consumer:
    name: Published consumer / ${{ matrix.fixture }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        fixture: [react-18, react-19, vue-3]
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: npm ci
      - run: node --check scripts/check-published-consumer.js
      - name: Install and build the published package
        run: >-
          node scripts/check-published-consumer.js
          "${{ inputs.version }}"
          "${{ matrix.fixture }}"
